# Host 白名单。默认留空，表示不限制访问地址；只有启用 Host 检查时才会生效。
# 公网反向代理域名请写入 web/.env.local，不要提交凭证。
DIRECTOR_ALLOWED_HOSTS=

# Web 开发服务器监听地址和端口。Docker 由 Nginx 监听 3000。
DIRECTOR_WEB_HOST=127.0.0.1
DIRECTOR_WEB_PORT=3000
